import React from 'react'
import ReactDOM from 'react-dom/client'
import TeleAdsApp from '../main'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TeleAdsApp />
  </React.StrictMode>
) 