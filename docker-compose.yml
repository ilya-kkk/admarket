version: '3.8'

services:
  bot:
    image: ${DOCKERHUB_USERNAME}/admarket-bot:latest
    env_file:
      - ./services/backend-bot/.env
    environment:
      - PYTHONUNBUFFERED=1
    depends_on:
      - frontend
    networks:
      - app-network
    restart: always
    healthcheck:
      test: ["CMD", "ps", "aux", "|", "grep", "python3"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    image: ${DOCKERHUB_USERNAME}/admarket-frontend:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./services/frontend/certificates:/app/certificates
    networks:
      - app-network
    restart: always

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data: 